/**
 * Site URL for sitemap, robots, and Open Graph.
 * Set NEXT_PUBLIC_SITE_URL in .env.local when you have a custom domain.
 */
export const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://strestimator.com";
